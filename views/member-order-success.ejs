<!DOCTYPE html>
<html lang="zh-TW">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>訂單成功</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-50">
    <div class="max-w-xl mx-auto px-4 py-16">
      <div class="bg-white rounded-lg shadow-lg p-8 mb-8">
        <div class="text-center mb-8">
          <svg
            class="mx-auto h-16 w-16 text-green-500"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M5 13l4 4L19 7"
            ></path>
          </svg>
          <h2 class="text-3xl font-bold text-gray-800 mt-6 mb-2">
            親愛的
            <span class="text-green-600"><%= locals.username %> </span
            >感謝你的購買!
          </h2>
          <p class="text-gray-500 mt-2">訂單已成功建立</p>
        </div>

        <div class="text-center border-t border-b border-gray-200 py-6">
          <p class="text-gray-600 mb-2">你的訂單編號：</p>
          <p class="text-2xl font-bold text-gray-900"><%= orderId %></p>
          <p class="text-sm text-gray-500 mt-2">請保存此編號以便日後查詢</p>
        </div>

        <div class="text-center mt-6">
          <p class="text-gray-600 mb-2">訂單金額：</p>
          <p class="text-2xl font-bold text-gray-900">
            HKD$ <%= totalAmount %>
          </p>
        </div>
      </div>

      <div class="flex justify-center gap-4">
        <a
          href="/"
          class="px-6 py-3 bg-green-500 text-white font-medium rounded-lg hover:bg-green-600 transition duration-150 ease-in-out"
        >
          返回首頁
        </a>

        <a
          href="/member/orders/lookup"
          class="px-6 py-3 bg-green-100 text-green-700 font-medium rounded-lg hover:bg-green-200 transition duration-150 ease-in-out"
        >
          查看訂單
        </a>
      </div>
    </div>
  </body>
</html>
