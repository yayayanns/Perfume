<!DOCTYPE html>
<html lang="zh-TW">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>訂單查詢</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 py-8">
      <h2 class="text-2xl font-bold mb-6">訂單查詢</h2>

      <div class="bg-white rounded-lg shadow p-6">
        <form method="GET" action="/orders/guest">
          <div class="mb-4">
            <label
              for="orderNumber"
              class="block text-gray-700 text-sm font-medium mb-2"
            >
              訂單編號
            </label>
            <input
              type="text"
              class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-green-500"
              id="orderNumber"
              name="orderNumber"
              value="<%= locals.orderNumber || '' %>"
              required
              placeholder="請輸入訂單編號"
            />
          </div>

          <% if (locals.error) { %>
          <div
            class="mb-4 p-3 bg-red-100 border border-red-400 text-red-700 rounded"
          >
            <%= error %>
          </div>
          <% } %>

          <div class="flex gap-4">
            <button
              type="submit"
              class="px-4 py-2 bg-green-500 hover:bg-green-600 text-white font-medium rounded-md transition duration-150 ease-in-out"
            >
              查詢訂單
            </button>

            <a href="/" class="px-4 py-2 text-green-600 hover:text-green-700">
              返回首頁
            </a>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
