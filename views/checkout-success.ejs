<!DOCTYPE html>
<html lang="zh-TW">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>訂單成功</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-50">
    <div class="max-w-xl mx-auto px-4 py-16">
      <h1 class="text-3xl font-bold text-green-600 text-center mb-8">
        訂單已成功建立！
      </h1>

      <div class="bg-white rounded-lg shadow-lg p-8 mb-8">
        <p class="text-gray-600 text-center mb-8">
          感謝您的購買，我們會盡快處理您的訂單。
        </p>

        <% if (orderNumber) { %>
        <div class="text-center border-t border-b border-gray-200 py-6">
          <p class="text-gray-600 mb-2">您的訂單編號：</p>
          <p class="text-2xl font-bold text-gray-900"><%= orderNumber %></p>
          <p class="text-sm text-gray-500 mt-2">請保存此編號以便日後查詢</p>
        </div>
        <% } %>
      </div>

      <div class="flex justify-center gap-4">
        <a
          href="/"
          class="px-6 py-3 bg-green-500 text-white font-medium rounded-lg hover:bg-green-600 transition duration-150 ease-in-out"
        >
          返回首頁
        </a>

        <a
          href="/orders/guest/lookup"
          class="px-6 py-3 bg-green-100 text-green-700 font-medium rounded-lg hover:bg-green-200 transition duration-150 ease-in-out"
        >
          查詢訂單
        </a>
      </div>
    </div>

    <!-- 用於調試的隱藏區域 -->
    <div class="hidden">
      Order Number Debug: <%= typeof orderNumber !== 'undefined' ? orderNumber :
      'undefined' %>
    </div>
  </body>
</html>
